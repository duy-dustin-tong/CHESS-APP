# Example frontend .env (Vite) for repository
# Copy to `.env` or set environment variables locally.

# Base API URL (do not include trailing slash)
VITE_API_URL=http://127.0.0.1:5000

# Socket server URL
VITE_SOCKET_URL=http://127.0.0.1:5000
